-- split/join blocks of code (arrays, hashes, statements, objects, etc)
return {
  {
    "wansmer/treesj",
    dependencies = {
      "nvim-treesitter/nvim-treesitter",
    },
    keys = {
      "<space>m",
    },
    opts = {},
  }
}
